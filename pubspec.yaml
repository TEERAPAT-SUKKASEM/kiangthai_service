name: kiangthai_service
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # ---------- Mr. Bug เพิ่ม 2 บรรทัดนี้ให้ครับ ----------
  supabase_flutter: ^2.0.0
  google_fonts: ^6.0.0
  image_picker: ^1.2.1
  # --------------------------------------------------

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
